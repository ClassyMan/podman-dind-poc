# Prefer mirror.gcr.io (Google's anonymous docker.io pull-through cache) to
# avoid Docker Hub's 100-pull/6h anonymous rate limit. Falls back to docker.io
# if the mirror doesn't have an image.
[[registry]]
location = "docker.io"

[[registry.mirror]]
location = "mirror.gcr.io"
